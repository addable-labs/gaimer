import { Dark, Notify, Dialog } from "quasar";

// The Quasar plugins the app installs; import a plugin and add it here.
// Each $q.<plugin> call needs its plugin in this list, e.g. $q.dialog needs
// Dialog. The tests install the same list when they mount components.
export const quasarPlugins = { Dark, Notify, Dialog };
